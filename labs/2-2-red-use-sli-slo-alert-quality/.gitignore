__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.log
# ...but experiment logs under artifacts/ are deliverables and must commit.
!artifacts/**/*.log
artifacts/**/tmp/
# Local-only host port overrides (see docker-compose.yml).
.env
.DS_Store
